name: "OM1 Binary Release"

on:
  push:
    branches: [ "main" ]
    tags: [ "v*" ]
  workflow_dispatch:
    inputs:
      version:
        description: "Version/tag for naming the archives (e.g. v1.2.3)"
        required: true
        default: "v0.0.0-dev"
      publish:
        description: "Create a GitHub Release and upload the archives"
        type: boolean
        default: false

permissions:
  contents: write

env:
  ZENOH_C_VERSION: "1.9.0"

jobs:
  setup:
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.v.outputs.version }}
      publish: ${{ steps.v.outputs.publish }}
      prerelease: ${{ steps.v.outputs.prerelease }}
    steps:
      - id: v
        shell: bash
        run: |
          set -euo pipefail
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            echo "version=${{ inputs.version }}" >> "$GITHUB_OUTPUT"
            echo "publish=${{ inputs.publish }}"  >> "$GITHUB_OUTPUT"
            echo "prerelease=false"               >> "$GITHUB_OUTPUT"
          elif [[ "${GITHUB_REF}" == refs/tags/v* ]]; then
            echo "version=${GITHUB_REF_NAME}"     >> "$GITHUB_OUTPUT"
            echo "publish=true"                   >> "$GITHUB_OUTPUT"
            echo "prerelease=false"               >> "$GITHUB_OUTPUT"
          else
            echo "version=nightly"                >> "$GITHUB_OUTPUT"
            echo "publish=true"                   >> "$GITHUB_OUTPUT"
            echo "prerelease=true"                >> "$GITHUB_OUTPUT"
          fi

  build-unix:
    name: Build ${{ matrix.name }}
    needs: setup
    runs-on: ${{ matrix.runner }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: linux-amd64
            runner: ubuntu-latest
            os: linux
          - name: linux-arm64
            runner: ubuntu-22.04-arm
            os: linux
          - name: darwin-arm64
            runner: macos-14
            os: darwin
          - name: darwin-amd64
            runner: macos-13
            os: darwin
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install build dependencies (Linux)
        if: matrix.os == 'linux'
        run: sudo apt-get update && sudo apt-get install -y portaudio19-dev pkg-config patchelf

      - name: Install build dependencies (macOS)
        if: matrix.os == 'darwin'
        run: brew install portaudio pkg-config

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
          cache: true

      - name: Cache zenoh-c
        uses: actions/cache@v4
        with:
          path: .zenoh-c
          key: ${{ runner.os }}-${{ runner.arch }}-zenoh-c-${{ env.ZENOH_C_VERSION }}

      - name: Build binary
        run: make build

      - name: Package archive
        id: pkg
        shell: bash
        env:
          VERSION: ${{ needs.setup.outputs.version }}
          PLATFORM: ${{ matrix.name }}
        run: |
          set -euo pipefail
          STAGE="dist/om1-${VERSION}-${PLATFORM}"
          mkdir -p "$STAGE"

          # Binary
          cp build/om1 "$STAGE/om1"

          # Bundle the zenoh-c shared library next to the binary and make the
          # binary look for it via a relative rpath.
          if [ "${{ matrix.os }}" = "linux" ]; then
            cp .zenoh-c/lib/libzenohc.so* "$STAGE/"
            patchelf --set-rpath '$ORIGIN' "$STAGE/om1"
          else
            cp .zenoh-c/lib/libzenohc.dylib "$STAGE/"
            # Drop the CI's absolute build-time rpath and add a relative one.
            install_name_tool -delete_rpath "$PWD/.zenoh-c/lib" "$STAGE/om1" 2>/dev/null || true
            install_name_tool -add_rpath @executable_path "$STAGE/om1"
          fi

          # Runtime data the binary expects to find via -config / knowledge base.
          cp -R config "$STAGE/config"
          [ -d knowledge_base ] && cp -R knowledge_base "$STAGE/knowledge_base" || true

          cat > "$STAGE/INSTALL.txt" <<EOF
          OM1 ${VERSION} (${PLATFORM})

          1. Install runtime prerequisites:
               macOS:  brew install portaudio ffmpeg
               Linux:  sudo apt-get install -y libportaudio2 ffmpeg

          2. Run:
               ./om1 -config config/conversation.json5

          The bundled libzenohc (in this folder) is loaded automatically via a
          relative rpath, so keep it next to the om1 binary.
          EOF

          ARCHIVE="om1-${VERSION}-${PLATFORM}.tar.gz"
          tar -C dist -czf "$ARCHIVE" "om1-${VERSION}-${PLATFORM}"
          shasum -a 256 "$ARCHIVE" > "${ARCHIVE}.sha256"
          echo "archive=${ARCHIVE}" >> "$GITHUB_OUTPUT"

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.name }}
          path: |
            ${{ steps.pkg.outputs.archive }}
            ${{ steps.pkg.outputs.archive }}.sha256
          if-no-files-found: error

  build-windows:
    name: Build windows-amd64
    needs: setup
    runs-on: windows-latest
    continue-on-error: true
    defaults:
      run:
        shell: msys2 {0}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up MSYS2 (mingw64)
        uses: msys2/setup-msys2@v2
        with:
          msystem: MINGW64
          update: true
          install: >-
            mingw-w64-x86_64-go
            mingw-w64-x86_64-gcc
            mingw-w64-x86_64-pkg-config
            mingw-w64-x86_64-portaudio
            curl
            unzip
            zip

      - name: Download zenoh-c (windows-gnu)
        run: |
          set -euo pipefail
          URL="https://github.com/eclipse-zenoh/zenoh-c/releases/download/${ZENOH_C_VERSION}/zenoh-c-${ZENOH_C_VERSION}-x86_64-pc-windows-gnu-standalone.zip"
          mkdir -p .zenoh-c
          curl -sSL -o /tmp/zenoh-c.zip "$URL"
          unzip -q -o /tmp/zenoh-c.zip -d .zenoh-c

      - name: Build binary
        run: |
          set -euo pipefail
          export CGO_ENABLED=1
          export CGO_CFLAGS="-I$(pwd)/.zenoh-c/include"
          export CGO_LDFLAGS="-L$(pwd)/.zenoh-c/lib -lzenohc"
          mkdir -p build
          go build -v -ldflags "-s -w" -o build/om1.exe ./cmd

      - name: Package archive
        id: pkg
        env:
          VERSION: ${{ needs.setup.outputs.version }}
        run: |
          set -euo pipefail
          PLATFORM="windows-amd64"
          STAGE="dist/om1-${VERSION}-${PLATFORM}"
          mkdir -p "$STAGE"

          cp build/om1.exe "$STAGE/om1.exe"
          cp .zenoh-c/bin/zenohc.dll "$STAGE/"

          # Bundle the mingw + portaudio runtime DLLs the exe links against.
          for dll in libportaudio*.dll libgcc_s_seh-1.dll libwinpthread-1.dll libstdc++-6.dll; do
            cp /mingw64/bin/$dll "$STAGE/" 2>/dev/null || true
          done

          cp -R config "$STAGE/config"
          [ -d knowledge_base ] && cp -R knowledge_base "$STAGE/knowledge_base" || true

          cat > "$STAGE/INSTALL.txt" <<EOF
          OM1 ${VERSION} (${PLATFORM}) - experimental build

          Run:
            om1.exe -config config\\conversation.json5

          Keep all bundled .dll files next to om1.exe.
          You may also need ffmpeg on your PATH for audio/video features.
          EOF

          ARCHIVE="om1-${VERSION}-${PLATFORM}.zip"
          (cd dist && zip -qr "../${ARCHIVE}" "om1-${VERSION}-${PLATFORM}")
          sha256sum "$ARCHIVE" > "${ARCHIVE}.sha256"
          echo "archive=${ARCHIVE}" >> "$GITHUB_OUTPUT"

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: windows-amd64
          path: |
            ${{ steps.pkg.outputs.archive }}
            ${{ steps.pkg.outputs.archive }}.sha256
          if-no-files-found: warn

  release:
    name: Publish Release
    runs-on: ubuntu-latest
    needs: [setup, build-unix, build-windows]
    if: ${{ needs.setup.outputs.publish == 'true' }}
    permissions:
      contents: write
    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Flatten artifacts
        run: |
          mkdir -p release
          find artifacts -type f \( -name "*.tar.gz" -o -name "*.zip" -o -name "*.sha256" \) -exec cp {} release/ \;
          ls -lh release

      - name: Move nightly tag to current commit
        if: needs.setup.outputs.version == 'nightly'
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh api -X PATCH "repos/${{ github.repository }}/git/refs/tags/nightly" \
            -f sha="${{ github.sha }}" -F force=true \
          || gh api -X POST "repos/${{ github.repository }}/git/refs" \
            -f ref="refs/tags/nightly" -f sha="${{ github.sha }}"

      - name: Create / update GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ needs.setup.outputs.version }}
          name: ${{ needs.setup.outputs.version == 'nightly' && 'Nightly (latest main)' || needs.setup.outputs.version }}
          prerelease: ${{ needs.setup.outputs.prerelease == 'true' }}
          generate_release_notes: true
          files: release/*
